interface MyInterface {
    id:number;           // you can't initialized only decleare.
    name:string;         // you can't initialized only decleare.

    dis1():void;         // you can't initialized only decleare.
    disInfo(): string;   // you can't initialized only decleare.
    
}